<h2>All Teams</h2>

<% School.joins(:teams).distinct.each do |campus| %>
  <ul class="collection with-header">
    <li class="collection-header"><h4><%= campus.city %></h4></li>
    <% campus.teams.each do |team| %>
      <li class="collection-item"><%= team.name %></li>
    <% end %>
  </ul>

<% end %>
